name: CI - Maven Derleme Süreci
on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]
jobs:
build:
runs-on: ubuntu-latest
services:
mysql:
image: mysql:8.0
env:
MYSQL_DATABASE: KampusApp
MYSQL_ROOT_PASSWORD: BE123456
ports:
- 3306:3306
options: >-
--health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -p2550"
--health-interval=10s
--health-timeout=5s
